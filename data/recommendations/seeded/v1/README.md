# Seeded recommendation fixture v1

The versioned manifest points to the committed mock CSV and records the
expected deterministic partition. Run `pnpm recommendations:seed` to parse,
validate, and optionally materialize a normalized JSON copy.
