> Approved planning revision: local POC with SQS Standard/Lambda (LocalStack), Redis opaque tokens, 20 leases / 300 seconds and 5 concurrent Flash Sale confirmations. Reopened tasks below reflect design changes, not completed implementation. Production CDN/private gateway, multi-instance deployment/acceptance, HA/Cluster and JWT rotation are out of scope. Existing unrelated checks retain their recorded status.

## 1. BE — Backend, contracts, infrastructure, and integration

BE owns shared contracts. `[Critical]` marks concurrency, authorization, state transitions, or multi-module transactional work that requires explicit review and regression evidence. Complete 1.1–1.3 before FE integration; waiting-room integration additionally depends on 1.29–1.31; FE can use the 1.2 fixtures while the remaining BE work proceeds. Priorities describe implementation order, not permission to omit later tasks.

- [x] 1.1 [P0] Verify campaign/CMS/pricing/inventory/order integration points against current code and document the affected modules and existing cancellation/online-reservation paths; preserve non-Flash-Sale behavior.
- [x] 1.2 [P0] Define shared seller SKU commands, public/private sale states, COD readiness, sale provenance, and stable error contracts; provide typed fixtures for upcoming, active, sold-out, ended, buyer-limit, and mixed-cart states for FE.
- [x] 1.3 [P0] [Critical] Add Prisma migration/constraints for SKU quota, fixed/reference prices, permanent buyer/product/campaign claims, per-line consumption/reversal, and command/outbox records; verify uniqueness, nonnegative quantities, and legacy order preservation.
- [x] 1.4 [P0] Add pinned local Redis runtime/client configuration, versioned script loading, bounded call timeouts, and the disabled-by-default SKU feature flag; document deferred recovery scope without deploying cloud services.
- [x] 1.5 [P0] [Critical] Implement centralized derived state using fresh server time and explicit per-SKU termination; test inclusive start, exclusive end, campaign cancellation, sold-out vs ended, and no reopening after end.
- [x] 1.6 [P0] [Critical] Implement atomic owned SKU registration with fixed-price validation, campaign policy, duplicate checks, protected physical backing, and idempotent all-or-nothing responses.
- [x] 1.7 [P0] [Critical] Implement quota-only pre-start editing, including after enrollment cutoff; retain enrollment/price cutoffs and reject all positive-quota active edits.
- [x] 1.8 [P0] [Critical] Implement zero-quota-only replenishment and per-SKU termination with expected versions, idempotent replay, unchanged price/claims, and backing checks; test replenish/end and cancellation/replenish races.
- [x] 1.9 [P0] Extend seller campaign detail with SKU rows, quota/stock values, and current action permissions; preserve ownership isolation and gate legacy product-only Flash Sale registration paths.
- [x] 1.10 [P0] [Critical] Extend inventory adjustment, ordinary reservation, online reacquisition, registration withdrawal, and campaign-end handling to protect/release remaining allocations without double physical deductions; test stock=quota and preexisting holds.
- [x] 1.11 [P0] [Critical] Reconcile Lua quota/claim/epoch admission with the five executing Flash Sale confirmation limit; preserve owned attempt compensation and do not reset running slots on TTL expiry.
- [x] 1.12 [P0] [Critical] Integrate pre-DB Redis admission with the bounded serializable COD transaction and common lock order; persist claims, consumption, quota, inventory, vouchers, and orders together; replay committed purchases before any new admission.
- [x] 1.13 [P0] [Critical] Implement postcommit token finalization and ordered exactly-once admission-delta outbox delivery alongside versioned absolute public snapshots; handle definitive rollback separately from ambiguous commit and prevent duplicate/stale events from overwriting newer quota state.
- [x] 1.14 [P0] [Critical] Integrate cancellation and seller rejection with original-participation reversal, exactly-once physical restoration, ongoing sale quota restoration, ordinary restoration after end, and permanent claims; cover cancellation/end/replenish races and later campaigns.
- [x] 1.15 [P1] [Critical] Extend central variant pricing/availability and bypass legacy product-wide discounts for FLASH_SALE; preserve ordinary siblings, unavailable sold-out state, price snapshots, and current ordinary pricing after end.
- [x] 1.16 [P1] [Critical] Extend cart quote/checkout preview with no quota mutation, one-unit product-level rules, COD-only selected purchases, and post-sale voucher eligibility; include sale identity/price/state in freshness checks without invalidating quotes solely because another buyer used a still-available unit.
- [x] 1.17 [P1] Guard online initiation/retry against newly evaluated sale lines before creating payment intents or holds; preserve ordinary-only purchases and valid preexisting normal-price orders.
- [x] 1.18 [P1] Update homepage, campaign detail, catalog/detail, search projection/hydration, and shared product mapping through the L1/L2 public cache (1.25–1.26) for eligible SKU prices/badges/boosts; omit buyer-facing exact sale counts and protect explicit sort semantics.
- [x] 1.19 [P1] Publish OpenAPI documentation for the endpoint inventory in design section 7, including exact DTOs, auth/origin/ownership, ETags/version, idempotency, response snapshots, and errors; run shared contract checks.
- [x] 1.20 [P1] Add API integration tests using real PostgreSQL and Redis for SKU commands, sibling/nonparticipating variants, cart/preview/COD, online guards, vouchers, buyer cancel, seller reject, and time boundaries.
- [x] 1.21 [P1] [Critical] Add reproducible contention/load tests for last quota, distinct buyers, same buyer across sibling SKUs and keys, mixed carts, stock backing, duplicate confirmations/cancellations, and seller/cancellation races; record invariant totals and latency without an unsupported throughput target. Added the fixture-driven k6 harness and post-run invariant verifier under `scripts/poc/t35`; execution remains tracked by 1.28/1.32.
- [x] 1.22 [P1] Add explicit SKU seed fixtures and migration/feature-disable verification; ensure quota-less legacy entries cannot produce live sale offers and existing order/claim/reversal data remain readable after disabling new admission.
- [x] 1.23 [P1] Run relevant BE/contracts formatting, lint, type-check, unit, integration, migration and build checks; record outcomes plus deferred Redis recovery criteria without claiming full original T35 closure.
- [x] 1.24 [P1] After BE/FE implementation and integrated verification, write the Vietnamese change-local flow.md and local runbook describing actual COD, quota, cancellation, replenishment/end behavior and measured checks; do not create flow.md during planning.

- [x] 1.25 [P0] [Critical] Implement bounded per-instance L1 -> Redis L2 -> controlled DB public reads with original age <=2 seconds, startsAt/endsAt validity, version fences, multi-instance invalidation, separate private responses, and no admission-counter writes from public fills.
- [x] 1.26 [P0] Implement campaign prewarming, distributed per-key public fill leases, process single-flight, bounded DB fill concurrency and the lightweight batched status endpoint (1–50 ids); publish timed-state/fresh-serverTime fixtures for FE.
- [x] 1.27 [P0] [Critical] Enforce a Redis atomic five-confirmation budget only for server-classified Flash Sale order creation; preview uses no slot, excess confirmation returns 429/Retry-After, same-key replay launches no duplicate execution and no process-local quota fallback is allowed.
- [ ] 1.28 [P1] [Critical] Run bounded three-phase POC checks on one API with real Redis/PostgreSQL; record cache/DB/confirmation counts, rejections, latency and visibility delay without production throughput claims.

- [x] 1.29 [P0] [Critical] Add LocalStack SQS Standard/Lambda Admission/Queue control plane and local static waiting-room integration; define authenticated join/status/cancel-waiting contracts and backend Flash Sale cart classification. Ordinary-only carts bypass admission; no production gateway/private ingress is required.
- [x] 1.30 [P0] [Critical] Implement Redis buyer/gate tickets with join Idempotency-Key and retry-safe SQS publication; handle duplicate/out-of-order SQS Standard delivery and atomically grant from a pool of 20 leases. Empty pool keeps tickets WAITING with bounded redelivery/backoff independent of polling; acknowledge only persisted grants or terminal tickets.
- [x] 1.31 [P0] [Critical] Keep fresh opaque random tokens in Redis with an immutable 300-second deadline and HttpOnly cookie delivery; validate buyer/session/gate/deadline. Add owned idempotent admitted relinquishment with immediate explicit leave, 5–10-second page-leave grace, matching heartbeat cancellation, stale lease/event fencing, deferred release during executing confirmation, and immediate waiting grants after the expiry reaper reclaims capacity. Hidden state, preview or failure alone does not release; heartbeat never renews the lease and TTL remains the lost-signal fallback.
- [ ] 1.32 [P1] [Critical] Verify LocalStack/Redis/PostgreSQL POC: 21 buyers/20 leases, six confirmations/five slots, duplicate joins/SQS messages, token mismatch, expiry during execution, success-expiry race, explicit leave, refresh within grace, one-of-many-tab closure, last-tab release, lost page-leave fallback, stale heartbeat/relinquish events, leave during execution, lost response/result lookup, ordinary-cart bypass and no duplicate orders. Keep order and join keys separate; polling/heartbeat does not invoke checkout/PostgreSQL or extend lease expiry.

## 2. FE — Seller workspace, buyer storefront, and integration journeys

Use `docs/ui-ux/t35-implement-sku-flash-sale.md`. FE 2.1–2.8 can use BE 1.2 contract fixtures; live API integration requires the corresponding BE endpoints. BE owns the final flow/runbook with FE journey evidence. No FE-local price/stock/claim authority is introduced.

- [x] 2.1 [P0] Add typed campaign/Flash Sale API client bindings and fixture-backed state mapping from BE 1.2; centralize authenticated errors, expected version, and stable retry keys.
- [x] 2.2 [P0] Extend Seller Campaigns with product-grouped SKU rows, current action permissions, private quota/stock values, loading/empty/error states, and the existing blue/white toolbar/table/footer layout.
- [x] 2.3 [P0] Build SKU selection/registration and pre-start quota-edit forms with fixed-price context, integer/backing validation, inline server errors, preserved input, and accessible modal focus.
- [x] 2.4 [P0] Build sold-out replenishment and per-SKU end dialogs; show only allowed actions, keep sale price read-only, refresh after success/conflict, and prevent duplicate submission without inventing success.
- [x] 2.5 [P1] Update shared buyer cards, homepage shelf, and campaign collection/detail for eligible variant representative prices and badges; remove public remaining-count/sold-progress UI and handle no active deals.
- [x] 2.6 [P1] Update product variant selection for one sale unit, sold-out sale SKU blocking, and ordinary sibling purchase; restore sale appearance on refreshed replenishment/cancellation and normal appearance after end.
- [x] 2.7 [P1] Update cart and checkout to show quota-derived readiness, persistent product-level purchase-limit messages, COD-only mixed selections, correct post-sale voucher breakdown, and changed-quote review without automatic substitution.
- [x] 2.8 [P1] Refresh sale state using the phase-specific polling in 2.15 on page return and countdown boundaries; preserve cart and user input after errors, and display COD confirmation as an order placed rather than payment received.
- [x] 2.9 [P1] Check existing admin Flash Sale type/placement preview and campaign navigation against the new contract; adapt only affected presentation and preserve independent CMS banner ownership.
- [x] 2.10 [P1] Add component tests for Seller quota gates/dialogs and Buyer SKU/badge/COD/voucher states, including sold-out surplus stock, canceled buyer claim, ordinary siblings, and stale responses.
- [x] 2.11 [P1] Integrate live BE commands and full SKU transitions with real local fixtures; verify quota edits, sold-out replenishment/end, cancellation restoration, and claim persistence without mocked success paths.
- [ ] 2.12 [P1] Add and run focused Playwright journeys after BE 1.20: register, buy COD, sell out, replenish, reject repeat buyer, buy ordinary sibling, cancel before/after end, expire campaign, and reject stale quote.
- [ ] 2.13 [P1] Verify Seller and Buyer layouts at mobile/tablet/desktop with keyboard/focus and reduced-motion checks; record visual evidence separately from component-test results.
- [x] 2.14 [P1] Run relevant FE formatting, lint, type-check, tests and production build; provide API integration and visual evidence to BE 1.24 and leave deferred recovery work explicitly out of the completion claim.

- [x] 2.15 [P1] Implement shared batched status polling with server-offset local countdown, 10–15-second pre-start and 3–5-second active/sold-out jitter, 0–500-ms start refresh, visible-tab gating, one in-flight request, deduplication, stop-on-end and Retry-After backoff; never auto-submit on restock.
- [ ] 2.16 [P1] Verify three-phase POC journeys on one API: stale responses, hide/return, ended polling, overload with preserved key/input, and observed replenishment/cancellation display delay.

- [x] 2.17 [P0] Update waiting/admitted/expired/closed UI for Flash Sale carts only; keep buyers waiting while pool is empty, preserve live ticket/cart/order key, show the immutable five-minute deadline, promise no FIFO position or stock, and never auto-submit. Add explicit admitted leave plus best-effort last-tab/page-leave relinquishment after 5–10 seconds, refresh/multi-tab heartbeat cancellation without renewal, hidden-tab safety and executing-confirmation deferral.
- [ ] 2.18 [P1] Integrate LocalStack control-plane cookie flow and backend result lookup; test explicit leave, refresh within grace, one-of-many-tab closure, last-tab release, hidden tab, lost page-leave signal, expired admission cycle, stale browser-instance events, sale end, ordinary-cart bypass, preview without confirmation slot, confirmation 429 retry, leave during execution and lost successful response.

