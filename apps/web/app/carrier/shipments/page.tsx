import { CarrierPortal } from '../../../components/carrier-portal';

/** The order-management view is available from both carrier entry points. */
export default function CarrierShipmentsRoute() {
  return <CarrierPortal />;
}
